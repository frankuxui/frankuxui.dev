---

---

<div
  class="w-full relative grid grid-cols-4 gap-x-4 gap-y-6 md:grid-cols-8 lg:grid-cols-12 lg:gap-x-6 mx-auto max-w-6xl mb-64 mt-10"
>
  {
    Array.from({ length: 6 }).map(() => (
      <div class="w-full h-full col-span-4 group mb-8 relative flex flex-col items-start gap-4 overflow-hidden rounded-2xl min-h-[504px] justify-end p-8 bg-foreground/5 animate-pulse">
        <div class="w-full gap-4 flex flex-col items-start justify-end h-full">
          <div class="h-5 rounded w-3/4 bg-foreground/15" />
          <div class="grid gap-3 w-full">
            <div class="h-2 rounded w-full bg-foreground/10" />
            <div class="h-2 rounded w-full bg-foreground/10" />
            <div class="h-2 rounded w-[98%] bg-foreground/10" />
            <div class="h-2 rounded w-[97%] bg-foreground/10" />
            <div class="h-2 rounded w-[45%] bg-foreground/10" />
          </div>
        </div>
      </div>
    ))
  }
</div>
